cin>>d;
int step;
step = d/5;
if(d%5==0)cout<<step;
else cout<<step+1;

....................................................

// (to solve a problem of game)
#include <stdio.h>
int main() {
    int n;
    
    printf("Enter a number: ");
    scanf("%d", &n); 
    if (n % 3 == 0) {
        printf("B wins\n");
    } else {
        printf("A wins\n");
    } 
    return 0;
}

.....................................................


// To find the largest element in the array

int largest = a[o];
for(int i=0 ; i<size , i++){
  if(a[i]>largest){
    largest = a[i];
  }
}
